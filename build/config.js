'use strict'

const path = require('path')

module.exports = {
  dev: {
    // Paths
    assetsSubDirectory: './static',
    assetsPublicPath: '/',
  },

  build: {
    assetsSubDirectory: './static',
    assetsPublicPath: '/',
  }
}
